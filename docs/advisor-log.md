# Advisor log

One line per shipped PR — PR · date · what changed and why it mattered. Newest last.

- PR #167 · 2026-07-26 · Suggest with AI shipped (operator-side reply drafting): draft ≠ send, notes never reach the model, `kind='suggestion'` metering excluded from the visitor quota; pre-PR adversarial review caught the in-flight composer race and empirically falsified the drizzle `.default()` INSERT-omission assumption (preview impact documented in the PR).
- PR #172 · 2026-07-26 · Homepage honesty pass after the live claim-check of the 883e7ca redesign: hero demo re-scripted around the real visitor-tap escalation, auto-escalation copy corrected, fabricated citation footers removed; showcase build guard + weekly Discord demo-health probe added (showcase key replacement still pending — task 125).
- PR #181 · 2026-08-02 · Voice KB grounding fix (audit item 0): the mint shipped the text path's 80k+-char prompt past the realtime 16,384-token cap and the widget warn-and-continued into live ungrounded calls — now a voice-sized budget + token-ceiling backstop server-side, and rejection/timeout is call-fatal ("unavailable") client-side; 6 adversarial-review findings fixed pre-merge, 8/8 mutants killed; upstream mint-time instruction binding filed as llmgateway#3377.
- PR #183 · 2026-08-02 · Voice budget relief reshaped to named projects (supersedes the workspace-scoped 661cd84 approach, co-authored with Haytham): only project ids listed in `VOICE_RAISED_LIMIT_PROJECTS` get raised still-fail-closed buckets (60/hr per project+IP, 500/day per project); the internal workspace class — including the published marketing dogfood key — keeps 4/20, because audit finding 5 showed a class exemption converts to up to $5k/day of anonymous public minting; 4/4 mutants killed, the finding-5 guard test being the one that fails if the workspace-scoped shape is ever restored.
- PR #184 · 2026-08-02 · Voice audit fast-follow (lands last in the series): per-workspace daily fleet bound — 100/day fail-closed, raised projects keep their vetted 500/day instead; realtime-spend line in the Discord digest (mints × the gateway's $10 session cap, the only honest local bound since voice rows carry zero cost); operator disclosure docs incl. "voice-enabled knowledge is visitor-visible" + the 8k/16k/12,288 budget numbers; positioning strings; two standing rules appended to AGENTS.md (voice-actions rail: server-side OTP/audit layer only, never realtime-native client tools; the #167 drizzle preview-safety correction); 11-agent adversarial review pre-merge — 3 findings fixed, 1 (bucket denial-burn) documented as pre-existing; 9/9 mutants killed.
- PR #185 · 2026-08-02 · Transcript hardening backport (closes #171): control/zero-width glyph neutralization extended to the summary/transcript paths + the buildTranscript slice(-0) fix (a -0 tail bound silently returned the whole array); also landed docs/advisor-log.md itself onto main with the #167–#184 entries.
- PR #187 · 2026-08-05 · requireRole deny-by-default actually enforced: a member row with a role string outside the enum had an undefined rank and `undefined < n` admitted it through every gate incl. owner-only billing — found by the invites Phase-1 census against prod's hand-inserted member rows; `?? 0` coercion + 8 middleware tests, mutant fails exactly the 4 malformed-role pins.
- PR #188 · 2026-08-05 · Invites PR-A (migrate-before-serve half, 0022/0025 precedent): hand-authored 0026 `workspace_invite` — token at rest only as SHA-256 hash, timestamp-modeled status, nullable created_by for the S6 scrub; schema.ts deliberately untouched; contract test pre-verifies PR-B's two atomic primitives at the SQL layer (single-use burn UPDATE replay = 0 rows, seat-guarded INSERT…SELECT no-ops at cap).
- PR #189 · 2026-08-06 · Invites PR-B (API): token-bearer accept per R2 (256-bit CSPRNG token, hash at rest — email-match unusable while #80 sits unmerged), the two-statement atomic accept (burn-first single-use UPDATE, then the seat-guarded INSERT…SELECT with un-burn compensation on a lost seat), and revoke made authoritative during the burn window — the real fix the M12 mutation miss surfaced; members management with last-owner/last-workspace guards and workspace-scoped read_status cleanup; canAddMember deleted (it bypassed the internal exemption); branched only after PR-A's 0026 was deploy-verified live (merge ec66974, Ploy "All 7 projects deployed successfully" 2026-08-05T09:23:42Z); 803 api tests, mutation table M1–M16 (M12 reported as the honest miss that exposed the revoke defect), three-reviewer round caught the Hono mount-prefix middleware bleed before it could 400 every accept in prod.
- PR #190 · 2026-08-06 · Invites PR-C (dashboard): MembersTab becomes the real roster ("Members of this workspace") — roles, seat usage, owner-gated admin minting, and the once-only invite link rendered solely from the create response (k1, test-pinned: no list or preview payload can reproduce it); /invite/[token] preview→accept page with per-code error states; the invite carried through sign-in/sign-up so a brand-new invitee lands on their invitation instead of the onboarding paywall; A1 analytics hardening — scrubUrl at both capture calls plus sanitize_properties on init keep the token out of $current_url/$referrer/$pathname on every event shape PostHog builds itself; 489 dashboard tests, mutants M17–M21 killed-exact.
- PR #193 · 2026-08-06 · Invite first-run fixes from the verification pass: the reconcile-vs-stale-cache demotion that stranded a fresh invitee on the onboarding paywall (accept now patches a provisional row + invalidates before selecting, and reconcile defers demotion while the list refetches — 9-mutant table killed-exact, live 7/7 without the localStorage pin the verification rig needed); the member-row identity collapse (dsInputClass w-full beating the appended w-28 — cn()/tailwind-merge + a min-w-24 floor); docs pricing derived at build via a remark plugin from shared billing-tiers so /llms.mdx and /llms-full.txt keep real figures and DISCOUNT_PERCENT=0 needs no copy edit — the first component-based attempt left unrendered tags on the AI-readable surfaces and was caught against the built output.
- PR #198 · 2026-08-07 · Invite OAuth carry (confirmed field repro: a Google sign-up lost the invite — Better Auth's fixed callbackURL drops the ?invite= param the email path rides, stranding the invitee on the paywall with the invite still pending): a tab-scoped single-use sessionStorage stash written by the social click + a boot check on /onboarding that hands the return over to /invite/<tok> behind a fallback notice; the pre-PR adversarial pass caught the error-retry wipe (errorCallbackURL now carries the invite so a retry re-stashes instead of clearing) and the stale-stash hijack (new-bot immunity, invite-page matching-clear, unauth stash→param conversion); 14/14 mutants killed, journey e2e red-proofed 3/3 against pre-fix code.
- PR #203 · 2026-08-07 · Visitor `clientId` stripped from every authenticated conversation response (closes #170): `(projectKey, clientId)` is the public `/v1/chat` identity pair, so the inbox list and thread GET were handing every workspace member the ability to post AS a visitor and have the bot's reply persisted and delivered to the real person — an omit-projection at both emission sites, the field dropped from the dashboard type, and pins asserting neither the key nor the value ever serializes (red-proofed per site); the pre-commit review pass also caught four stale typed fixtures that broke `tsc` invisibly to vitest, which strips types without checking.
- PR #204 · 2026-08-07 · Stripe.js hosts allow-listed in the dashboard's report-only CSP (closes #196): the policy had no `frame-src` at all, so Stripe's controller iframe fell through to `default-src 'self'` and logged a violation on every billing visit — added exactly the documented Stripe.js set (`js.stripe.com` + `*.js.stripe.com` script/frame, `hooks.stripe.com` frame, `api.stripe.com` connect), plus the api origin in `script-src` for the dogfooded widget script, which was the next reporter waiting and would have broken outright the day this policy is promoted to enforcing.
- PR #205 · 2026-08-07 · Every session 401 now carries `code: "unauthorized"` (closes #195): `requireSession`, `requireGlobalAdmin` and the `/admin/me` probe were the only error surface where the code WAS the message, leaving clients to string-match while everything else branches on a machine-readable code; behaviour is unchanged (`ApiError` computes `code ?? error`, so the value was already `"unauthorized"`), the Svix webhook's invalid-signature 401 is deliberately untouched, and a pin now fixes the body shape.
- PR #207 · 2026-08-07 · Admin usage KPIs split by `usage_event.kind` (closes #169): the console's "AI responses" had silently mixed visitor replies with operator-side Suggest-with-AI drafts and voice mints since #167, so overview and workspaces now name each class apart — folds extracted to pure tested helpers, `USAGE_KINDS` derived from the schema enum so a future kind can't vanish from every bucket, and unknown kinds held out of the named counts but inside the displayed all-kind total, making the gap the tell. The issue's premise that costs are "real inference spend" was falsified in review: all three writers insert `costUsd: 0`, so every cost figure the console has ever shown is structurally $0 — those surfaces now read "not metered yet" and point at #206 rather than continuing to imply spend.
- PR #209 · 2026-08-07 · The api suite stops depending on a gitignored build artifact (closes #186), and #186 itself was a misdiagnosis: the reported "1 test file failed while every test passes" was never an escaped unhandled rejection — `cors.test.ts` is the only test importing the whole app, the app statically imports `src/generated/widget-bundle.ts` (emitted only by the widget build), so in any fresh worktree that file cannot COLLECT. The issue's own numbers were the answer: cold runs reported FEWER tests than warm ones, which a runtime fault never causes. Vitest now stands in an empty bundle only when the artifact is absent (inert otherwise, and `widget-asset.ts` already degraded to its PLACEHOLDER for exactly this case); 13 instrumented full-suite runs found zero unhandled rejections, and the same missing artifact's effect on `tsc` is filed as #208.
- PR #210 · 2026-08-09 · The widget launcher derives a contrast-safe foreground from the brand color (closes #145): the launcher glyph, its focus ring, and the unread badge printed literal white on `var(--brand)`, so a pale brand (`#fef08a` — 1.16:1 against white) shipped an invisible icon; the frame now picks ink-or-white at mount by comparing true WCAG contrast ratios (`contrast.ts`), publishes it as `--brand-fg`, and the badge inverted for free — its digits stay `var(--brand)` on a `var(--brand-fg)` pill, readable in both directions by construction. Per the decision: derive only, no save-time validation, no constraint on what brand a customer may choose. The mutation table earned its keep — a fixed luminance>0.5 cutoff passes every example case including the pale-yellow one, and only the gray-ramp property test kills it. Residuals filed: the header/visitor-bubble/send/jump/identify surfaces still on literal `#fff` are #211; the widget-rsc launcher, whose brand can arrive as host CSS with no mount-time value to derive from, is #212.
- PR #214 · 2026-08-09 · The dashboard now tells a voice-entitled operator when a project's prompt + knowledge outgrow what a voice call carries (closes #182): the mint's entire assembly moved to `lib/voice-instructions.ts` and the new `GET /api/projects/:id/voice-budget` runs the SAME functions on the SAME data — server-side on purpose, since the active prompt-library row isn't in the dashboard's cache, which would make a client-side estimate wrong for exactly the operators most likely to overflow. The pre-PR spec review falsified the first cut: it measured the post-slice assembly, so the operator #182 exists for (100k of knowledge silently sliced to 8k) would never have seen the hint — the shipped version compares the budgeted assembly against an unbudgeted one through buildSystem itself and reports the raw-content estimate, so the char budgets that fire long before the token ceiling are visible too. Signal only, nothing gated; the locked rider disclosure ("voice sessions deliver your project's prompt and sources to the caller's browser") ships verbatim on both surfaces, never hidden behind a slow budget query. Two first-table mutants survived by test self-reference (the boundary derived from the constant under test, an assertion that moves with any mutation) — rewritten with literal numbers and an integer-exact 90% mark; final table 9/9 killed.
- PR #213 · 2026-08-10 · The unread badge ported to widget-rsc (closes #147), pinned to #144's shipped behavior rather than reinvented: the same `llmchat_unread_<projectKey>` sessionStorage marker (a last-seen sequence, never a count), background polling gated on conversation-exists AND escalated-and-unresolved at 25s (inside the 20–30s band, ≥8× the foreground cadence), zero requests on a fresh mount, the "9+" cap, the RTL `direction: ltr` pin, and the post-stream retry that covers the waitUntil persistence race. The port's one real divergence is named: widget-rsc never rotates clientId, so a `feedLoaded` boolean replaces #144's feedClientId stamping. The ported tests caught their own rig twice — `seedTab()` without an argument silently seeded nothing (eight tests green for the wrong reason), and the all-microtask chat mock collapsed the immediate-refresh path into the retry path until a held SSE stream let operator rows land mid-answer — and the pre-existing primitives stub had to learn that `/v1/chat` CREATES the conversation, or the new deletion-reset effect read every canned reply as an operator delete. The #210 `--brand-fg` derive does not port (brand can arrive as host-set CSS with no mount-time value), so the badge keeps literal `#fff` with a comment citing #212.
- PR #215 · 2026-08-10 · Conversation assignment PR-A (#96, docs/goals/assignment.md): migration 0027 creates `conversation_assignment` — Option B as census-argued (the #167 correction strips an undeclared column from every drizzle projection, so Option A's supposed serialization/stats advantages were illusory; a new TABLE is preview-safe by construction per the 0026 precedent), with the Phase-2 lock's one amendment to the draft stated per house rule: `conversation_id` IS the primary key (surrogate id + unique index dropped), so the PK itself enforces max-one-assignee and claim/reassign become `ON CONFLICT DO NOTHING`/`DO UPDATE`. A 9-test contract suite replays the real SQL before any consumer exists — column shape, index, FK declarations, first-wins claim, last-write-wins upsert, idempotent unassign — and surfaced a harness truth worth banking: the 0007/0011/0012 rebuild migrations end with `PRAGMA foreign_keys=ON`, so the delete-e2e's initial OFF never survives its own migration loop (it never noticed, because it seeds real parent rows). schema.ts deliberately untouched until PR-B; prod deploy status to be recorded on the PR before PR-B branches.
- PR #216 · 2026-08-10 · Conversation assignment PR-B (#96, docs/goals/assignment.md): the api grows its first workflow-metadata surface — PUT …/assignee carries assign/reassign/unassign in one route (A1: an explicit null unassigns, an absent field 400s, so a partial payload can never silently unassign), claim-on-reply lands AFTER the successful insert and BEFORE the throwing email send as a first-wins `ON CONFLICT DO NOTHING`, the me/none filters ride the tag-filter's ANDed-predicate slot so cursor/status/search/tags all compose, and the stats row gains caller-scoped mine/unassigned counts with zero extra queries. R5 stayed authored, never inherited: every exposure is an explicit join, and the two operator row-spreads the census flagged are ticketed (#217) rather than silently widened. The 13-mutant table earned its keep in the strongest way yet: M9 (drop the cascade's assignment delete) SURVIVED, exposing that the delete-e2e's `PRAGMA foreign_keys=OFF` premise has been silently defeated since migration 0007 — the 0007/0011/0012 rebuilds end with `PRAGMA foreign_keys=ON`, so its "children die by OUR explicit deletes, never the FK cascade" proof hadn't actually been testing that for months. The fix now lives ONCE in the shared `src/test/sqlite-rig.ts` — a harness-integrity repair that outlives this feature. Two-axis review: zero blockers; its three test-depth gaps (sendEmail-throw claim survival, real-SQL conversation-DELETE cleanup, search×/tags×assignee composition) all closed pre-merge.
- PR #219 · 2026-08-10 · Conversation assignment PR-C (#96): the dashboard's dimmed stubs graduate — the Assignee radiogroup (All/Mine/Unassigned) joins Status as a COMPOSING axis with real caller-scoped pill counts per the lock (a stale cached response renders no count, never a filler), the AssigneePicker replaces the ThreadActions stub in TagPicker's exact Popover+Command shape ("Assign to me" leads, roster from the SHARED members cache so no new endpoint, Unassign only when assigned, assigned-by as the locked cheap title tooltip), and the assignee chip rides the list rows + a DetailPanel row. Optimistic assign/unassign uses the house wide-prefix pattern with head-only revalidation and rollback; success is deliberately silent — the chip IS the feedback. One deviation named and RATIFIED: no optimistic self-claim mirror on reply, because a mirrored claim on a reply that then FAILS would show a false chip for a poll cycle — the truthful chip arrives on server confirm via the existing head invalidation. Dashboard 535/535, 5/5 mutants killed; the default inbox request stays byte-identical to pre-#96.
- PR #221 · 2026-08-10 · The auto-sign-out bug root-caused and killed on both axes: Better Auth's once-per-updateAge refresh emits its re-stamped cookie INSIDE getSession, and requireSession — the busiest caller by orders of magnitude (the inbox's ~5s poll) — won the once-a-day race and discarded the headers, so every browser cookie kept its sign-in Max-Age and hard-expired 7 days later while the DB row stayed valid; the fix forwards the re-stamp through a shared `forwardAuthCookies` (which also stamps `no-store` on any token-carrying response), and the review pass caught the admin surface carrying the identical bug — `/admin/me`, the admin frontend's poll target, was the likeliest consumer. The second axis: the 1.6.9 client nulls its session store on ANY non-2xx focus refetch, and the always-armed client gate (prod SSR never sees the api-origin cookie) rendered every 429/5xx blip as a sign-out — gates now demand a definitive signal (clean 200-null or real 401) via one predicate, and the zero-workspaces bounce demands an actually-loaded list. The PR2a fail-closed rule gets its narrowest carve-out: only `…|/get-session` keys fail open (verified against createRateLimitKey's `${ip}|${path}` derivation — traversal tricks either resolve to get-session itself or keep the credential key), unknown shapes degrade toward MORE denial. 12/12 mutants killed; the whole-chain e2e drives the REAL Better Auth runtime through the real middleware.
- PR #222 · 2026-08-10 · better-auth 1.6.9 → 1.6.26, deliberately stacked on #221 so the bump had to pass the new whole-chain refresh e2e before it could land: seventeen patch releases of session/cookie fixes (get-session no-store, focus-refetch dedupe + rate-limiter concurrency, __Secure- preference over stale non-secure leftovers, refresh Max-Age cap, D1 affected-row counting). The find worth the line: upstream closed PR2a's pre-registration takeover natively — the implicit-link gate now requires the EXISTING local account's verified email (`requireLocalEmailVerified ?? true`), the exact check whose absence made 1.6.9's link decision exploitable — so the linking CONTRAST test flips from proving the vulnerability exists to PINNING the upstream guard against silent regression in future bumps, with our disableImplicitLinking retained as defense-in-depth (it still refuses even verified-local links). One preview flake on the way in (1-of-7, the known lockfile-branch class; identical code re-deployed clean).
- PR #223 · 2026-08-11 · The widget learns RTL the honest way (dir="auto" on every message bubble + the composer), found because marketing needed it true before filming it: the 12-language demo's money shot — an Arabic answer visibly reflowing right-to-left — didn't exist, since the bubbles had zero direction handling and Arabic rendered as RTL glyphs trapped in an LTR paragraph. First-strong-character resolution means each bubble follows its OWN content (Arabic right-aligns, English stays LTR, per-message in one conversation) and the input flips live as a visitor types Arabic — a real fix for every Arabic/Hebrew/Farsi/Urdu visitor that happens to also make the demo filmable. Verification note: the language auto-match claim itself was probe-verified on prod first (es/ar/Darija-in-Arabizi, one conversation, KB-grounded answers with citations) — the shoot only records what the product already does.
