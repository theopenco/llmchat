# Admin console environment — copy to apps/admin/.env.
# NEXT_PUBLIC_* values are baked into the browser bundle — public only, no secrets.

# Origin of the API / Better Auth server the console calls.
NEXT_PUBLIC_API_URL=http://localhost:8787
