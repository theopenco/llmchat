# Workspace-level config for `ploy dev`.
# `ploy dev` from the repo root boots every project (api worker, dashboard
# Next.js, marketing Next.js). Per-project ports come from each ploy.yaml's
# `dev:` block.

dashboard:
  port: 9787

ports:
  worker:
    from: 8787
